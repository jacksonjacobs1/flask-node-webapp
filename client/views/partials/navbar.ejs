<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">Quick Annotator</a>
    <ul class="navbar-nav mr-auto">
        <% navbarItems.forEach(item => { %>
            <li class="nav-item">
                <a class="nav-link" href="<% item.link %>">
                    <%= item.label %>
                </a>
            </li>
        <% }) %>
    </ul>
</nav>